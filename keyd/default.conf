# ⚠️ STATUS: UNMAINTAINED
# This keyd configuration is kept for reference only. 
# It has been replaced by the xremap configuration to keep 
# all custom shortcuts in a single unified layer.

[ids]

*

[main]

# Maps capslock to escape when pressed and control when held.
# capslock = overload(control, esc)

# Remaps the escape key to capslock
# esc = capslock

[alt]
t = C-t
r = C-r
w = C-w
k = C-tab
j = C-S-tab
